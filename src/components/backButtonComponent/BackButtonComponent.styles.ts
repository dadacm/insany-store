import styled from 'styled-components';

export const BackButton = styled.a`
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 16px;
  color: #617480;
  font-weight: 500;
  font-family: var(--font-saira);
  margin-bottom: 22px;
  cursor: pointer;
`;
